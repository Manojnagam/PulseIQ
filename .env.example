# ── Groq (server-side — NEVER expose to browser) ──
GROQ_API_KEY=gsk_...

# ── Supabase (publishable — safe in client, but keep here for edge functions) ──
SUPABASE_URL=https://erteibdxzdvsaujptxsd.supabase.co
SUPABASE_ANON_KEY=eyJ...
# Service role key — only for Supabase Edge Functions (never send to browser)
SUPABASE_SERVICE_ROLE_KEY=eyJ...
